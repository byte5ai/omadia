# Plugin manifests directory

The middleware loads plugin manifests from this directory at boot. Drop `<plugin-id>.manifest.yaml` files here to register external plugins without going through the ZIP upload flow.
