{
  "name": "@omadia/diagrams",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Diagram rendering library (Kroki + Tigris + signed URLs) for the Omadia middleware. Library-phase — tool + route live in the kernel until plugin-izing lands.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1032.0"
  },
  "peerDependencies": {
    "@omadia/plugin-api": "*",
    "express": "^5.1.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
