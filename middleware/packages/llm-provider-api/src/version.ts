/**
 * Contract version. A provider/adapter plugin compiled against this package
 * declares which contract it implements; the host can compare and refuse a
 * mismatch at install. Bump MAJOR on a breaking change to any exported type in
 * this package, MINOR on a backward-compatible addition.
 */
export const LLM_PROVIDER_API_VERSION = '1.0.0' as const;
