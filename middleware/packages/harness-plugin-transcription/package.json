{
  "name": "@omadia/plugin-transcription",
  "version": "0.1.0",
  "private": true,
  "description": "Audio-Ingestion-Plugin für die transcription@1-Capability (#584): operator-authentifizierter Multipart-Upload-Endpoint (/transcriptions) für Aufnahmen in den Blob-Store plus das explizite transcribe_recording-Tool — kanonisches Transcript-Artifact (unmaskiert, Blob-Store), maskierte Chunk-Projektion in die Session-Substrate.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'test/**/*.test.ts'"
  },
  "dependencies": {
    "music-metadata": "^11.15.0"
  },
  "peerDependencies": {
    "@omadia/orchestrator": "*",
    "@omadia/plugin-api": "*",
    "@omadia/plugin-privacy-guard": "*",
    "@omadia/transcription-api": "*",
    "@omadia/usage-telemetry": "*",
    "express": "^5.1.0",
    "multer": "^2.1.1",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
