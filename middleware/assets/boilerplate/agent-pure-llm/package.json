{
  "name": "{{AGENT_ID}}",
  "version": "{{AGENT_VERSION}}",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "description": "{{AGENT_DESCRIPTION_DE}}",
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
